spring:
  application:
    name: ms-api-gateway
  profiles:
    active: ${APP_PROFILE:dev}

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  tracing:
    sampling:
      probability: 1.0